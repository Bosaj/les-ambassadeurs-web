Contributing

1. Fork and clone
2. git checkout -b feat/your-feature
3. npm install && npm run dev
4. Make changes with tests
5. npm run lint && npm test
6. Commit: feat: description
7. PR to main

All UI text needs 3-language translations (ar/fr/en) in src/translations.js